apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-categorie
  namespace: prod
spec:
  selector:
    matchLabels:
      app: ms-categorie
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 100%
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: ms-categorie
    spec:
      containers:
        - name: ms-categorie
          volumeMounts:
            - mountPath: "/usr/src/app/static/pictures"
              name: static-files
          image: mathiskrr/breizhsport-categorie:develop
          env:
            - name: PORT
              value: "4003"
            - name: DB_HOST
              value: db-categorie
            - name: DB_DIALECT
              value: "mysql"
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: ms-categorie
                  key: db-name
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: ms-categorie
                  key: db-user
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ms-categorie
                  key: db-password
          ports:
            - containerPort: 4003
          resources:
            requests:
              cpu: 100m
              memory: 150Mi
            limits:
              cpu: 150m
              memory: 200Mi